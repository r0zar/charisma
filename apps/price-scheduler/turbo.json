{
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ],
      "env": [
        "HIRO_API_KEY",
        "BLOB_READ_WRITE_TOKEN",
        "CRON_SECRET"
      ]
    }
  }
}