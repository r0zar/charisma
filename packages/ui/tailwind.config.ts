import type { Config } from 'tailwindcss';
import theme from './tailwind.theme';

const config: Config = {
    content: [], // Will be defined in each app's config
    theme,
    plugins: []
};

export default config; 